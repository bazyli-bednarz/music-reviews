<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top ps-3 pe-3">
    <a class="navbar-brand d-inline-block" href="/">Music reviews</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="/"><span class="material-symbols-outlined">home</span>{{ 'nav.home'|trans }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ url('album_index') }}"><span class="material-symbols-outlined">album</span>{{ 'nav.albums'|trans }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ url('artist_index') }}"><span class="material-symbols-outlined">mic_external_on</span>{{ 'nav.artists'|trans }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ url('category_index') }}"><span class="material-symbols-outlined">category</span>{{ 'nav.categories'|trans }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ url('user_index') }}"><span class="material-symbols-outlined">diversity_3
                    </span>{{ 'nav.users'|trans }}</a>
            </li>
        </ul>
    </div>
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <div class="bg-dark text-light me-2"><a href="{{ url('user_show', {slug: app.user.slug}) }}">{{ app.user.username }}</a></div>
        <div>
            <a href="{{ url('app_logout') }}">{{ 'action.logout'|trans }}</a>
        </div>
    {% else %}
        <div>
            <a href="{{ url('app_login') }}">{{ 'action.login'|trans }}</a>
        </div>
        <div>
            <a class="ms-1" href="{{ url('app_register') }}">{{ 'action.register'|trans }}</a>
        </div>
    {% endif %}
</nav>