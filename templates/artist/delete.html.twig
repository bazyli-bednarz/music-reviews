{% include 'default/_form.html.twig' with {
    page_title: 'title.artist_delete'|trans({'%slug%': artist.slug|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'artist_index',
} %}
