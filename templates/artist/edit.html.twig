{% include 'default/_form.html.twig' with {
    page_title: 'title.artist_edit'|trans({'%slug%': artist.slug|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'artist_index',
} %}